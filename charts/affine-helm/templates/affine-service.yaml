apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -c
    kompose.version: 1.37.0 (fb0539e64)
  labels:
    io.kompose.service: affine
  name: affine
spec:
  ports:
    - name: "http"
      port: 3010
      targetPort: 3010
  selector:
    io.kompose.service: affine
