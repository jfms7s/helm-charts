apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-redis
  labels:
    {{- include "affine-helm.labels" (dict "context" $ "name" "redis" "component" "cache") | nindent 4 }}
spec:
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
  selector:
    {{- include "affine-helm.selectorLabels" (dict "context" $ "name" "redis") | nindent 4 }}
